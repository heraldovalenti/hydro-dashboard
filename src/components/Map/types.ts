export type Coordinate = {
  lat: number;
  lng: number;
};

export type StrokeAttributes = {
  strokeColor: string;
  strokeOpacity: number;
  strokeWeight: number;
};

export type FillAttributes = {
  fillColor: string;
  fillOpacity: number;
};
